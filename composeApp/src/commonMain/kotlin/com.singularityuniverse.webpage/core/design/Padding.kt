package com.singularityuniverse.webpage.core.design

import androidx.compose.foundation.layout.PaddingValues

object Padding {
    val Chicken = PaddingValues(Size.Chicken)
    val ExtraSmall = PaddingValues(Size.ExtraSmall)
    val Small = PaddingValues(Size.Small)
    val Medium = PaddingValues(Size.Medium)
    val Large = PaddingValues(Size.Large)
    val ExtraLarge = PaddingValues(Size.ExtraLarge)
    val Wow = PaddingValues(Size.Wow)
    val UnbelieveAble = PaddingValues(Size.UnbelieveAble)
    val Steve = PaddingValues(Size.Steve)

    // im not sure if you need this but just in case
    val Government = PaddingValues(Size.Government)
}
